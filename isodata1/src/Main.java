import java.util.ArrayList;

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.util.ToolRunner;


public class Main {
	public static ArrayList <Pixel> c_centers = new ArrayList <Pixel>();
	/**
	 * @param args
	 */
	public static void main(String[] args)throws Exception {	int res;		
	//res = ToolRunner.run(new Configuration(), new isodata_algorithm(w), www);
	//isodata_algorithm.width = Integer.valueOf(args[0]);
	//isodata_algorithm.height = Integer.valueOf(args[1]);
//	int res1 = ToolRunner.run(new Configuration(), new isodata_algorithm(), args);
//	System.exit(res1);
	MyFrame a = new MyFrame ("MyFrame");
	
}
}
